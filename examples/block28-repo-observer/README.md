# Block 28 repo observer fixtures

These fixtures document the first-mile repository observer status shape.

They are structural examples only. They do not claim that checked-in JSON is live
proof. The authoritative status is produced by `sdp-trace doctor --profile
github-actions-git-hooks-v1 --out <file>` against the current repository.
