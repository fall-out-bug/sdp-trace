# Authority Envelope Fixtures

These fixtures show how `sdp-trace` evaluates observed actions against a caller-selected authority envelope.

They are structural examples. They do not decide whether an `outside_authority` fact blocks a merge, contaminates a demo, or requires escalation. Those consequences belong to an external policy consumer.
