# GitHub SpecKit Example

State: `placeholder`.

This directory is not first-class MVP evidence. It is reserved for a future
fixture showing how a SpecKit-style spec, plan, and task set maps to an
`sdp-trace` evidence bundle and external verdict input.
