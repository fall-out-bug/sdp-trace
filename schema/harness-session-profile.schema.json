{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.sdp.dev/trace/harness-session-profile.schema.json",
  "title": "SDP Trace Harness Session Profile",
  "type": "object",
  "required": [
    "schema_version",
    "profile_id",
    "harness_profile_path",
    "event_source_path",
    "stream_capture"
  ],
  "properties": {
    "schema_version": { "const": "harness-session-profile-v1" },
    "profile_id": { "$ref": "#/$defs/safeId" },
    "harness_profile_path": { "$ref": "#/$defs/relativeFile" },
    "event_source_path": { "$ref": "#/$defs/relativeFile" },
    "raw_event_source_path": { "$ref": "#/$defs/relativeFile" },
    "raw_event_format": { "enum": ["opencode-jsonl-v1"] },
    "setup_actions": {
      "type": "array",
      "maxItems": 3,
      "items": { "$ref": "#/$defs/setupAction" }
    },
    "isolation_rules": {
      "type": "array",
      "items": { "$ref": "#/$defs/isolationRule" }
    },
    "stream_capture": {
      "enum": ["disabled", "digest_only", "retained_safe"]
    }
  },
  "dependentRequired": {
    "raw_event_source_path": ["raw_event_format"],
    "raw_event_format": ["raw_event_source_path"]
  },
  "$defs": {
    "safeId": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_.:-]{1,128}$"
    },
    "relativeFile": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_./:-]{1,256}$",
      "not": {
        "anyOf": [
          { "pattern": "^/" },
          { "pattern": "\\.\\." },
          { "pattern": "://" }
        ]
      }
    },
    "setupAction": {
      "type": "object",
      "required": ["id", "kind", "required"],
      "properties": {
        "id": { "$ref": "#/$defs/safeId" },
        "kind": { "enum": ["init", "profile", "wrapper", "hook", "context_isolation"] },
        "required": { "type": "boolean" }
      },
      "additionalProperties": false
    },
    "isolationRule": {
      "type": "object",
      "required": ["id", "kind", "target_path", "pattern", "required"],
      "properties": {
        "id": { "$ref": "#/$defs/safeId" },
        "kind": { "enum": ["ignore_line", "json_read_deny"] },
        "target_path": { "$ref": "#/$defs/relativeFile" },
        "pattern": {
          "type": "string",
          "minLength": 1,
          "maxLength": 256,
          "not": { "pattern": "[\\r\\n]" }
        },
        "required": { "type": "boolean" }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
