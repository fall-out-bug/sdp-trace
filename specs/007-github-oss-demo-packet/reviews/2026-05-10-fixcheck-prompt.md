# Fix-Check Prompt: GitHub OSS Demo Packet

Review only the fixes after the Socratic review.

Changed risk areas:

- existing demo repo remains default;
- first-packet selection and minimum bar;
- Feature 4 contamination handling;
- retroactive task provenance and artifact retention;
- negative theater PR is a GitHub draft PR with `agent_claimed_verification`;
- PR artifact is canonical; PR comment/body link is informational;
- 007 demo packets are generated by the 006 renderer and validated by the 006
  validator;
- product implementation tasks moved to 006.

Return:

```text
Verdict: <FIXES_ACCEPTED|REVISE_FIXES|NOT_ASSESSED>

Findings:
| id | severity | file/section | finding | exact fix |

Prior major/critical status:
<closed/open list>
```

Do not reopen unrelated product questions unless the fix introduced a critical
or major contradiction.
