```text
Verdict: REVISE_BEFORE_USER_APPROVAL

Findings:
| id  | severity | file/section                      | finding                                                                                                                                                                                                                                                               | exact fix                                                                                                                                                                                                                                                                                                                                                                 |
|-----|----------|-----------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| G1  | critical | spec.md / Required Demo Evidence  | "Retained task artifact" is listed as a valid task-source alternative, but no definition exists for what makes a task artifact retainable or resolvable. A PR body task section can be edited post-hoc, which silently backdates provenance.                           | Add a provenance rule: retroactive task binding MUST record the binding commit SHA and timestamp in the packet metadata. A PR body task section added after the feature PR merged MUST cite the binding commit. The packet `PC-INITIATOR` row state for retroactively-bound tasks MUST be `harness_observed` at best, never `change_host_observed`.                        |
| G2  | critical | demo-repo-plan.md / Feature PR Requirements | GitHub Actions artifacts have a default 90-day retention limit. The existing demo repo's CI artifacts from prior feature runs may already be expired. The plan assumes "retained artifact" is available for retroactive packets without addressing retention.          | Add explicit artifact-retention rules: (a) retroactive packets for expired CI artifacts MUST record `cannot_verify` with reason `artifact_expired`; (b) the setup PR MUST configure artifact retention to at least 180 days for demo packets/bundles; (c) the first retroactive packet MUST inspect actual artifact availability before claiming `pass` or `partial`.       |
| G3  | major    | spec.md / Demo Track Policy       | "Select one existing feature PR or feature history" as first packet target, but no selection criteria are defined. Without criteria, the choice is arbitrary and can be optimized for prettiness rather than evidence richness.                                        | Add selection criteria: choose the PR with the most available GitHub evidence surface (non-expired CI, linked commits, review comments, non-empty PR body). Document the selection rationale in the packet metadata `generated_from` field. If no existing PR has sufficient evidence, the first packet MUST come from a new v2 PR, not a retroactive backfill.             |
| G4  | major    | demo-repo-plan.md / Negative Theater PR | "Draft PR or retained fixture" are listed as interchangeable alternatives, but their evidence surfaces differ fundamentally. A draft PR has GitHub CI/check surface; a retained fixture is local-only. The CTO demo script assumes GitHub-visible theater evidence.  | Fix: the negative theater demo MUST be a GitHub draft PR with CI that runs but produces no qualifying verification artifact. Remove "retained fixture" as an equivalent alternative for the demo. Retained fixtures may exist for testing, but they are not CTO-demo proof.                                                                                                |
| G5  | major    | plan.md / A5                      | PR projection says "optionally post/update a PR comment with a short packet summary." "Optional" means the CTO demo might have no PR-visible packet pointer, breaking the demo script's step "Open `change-evidence-packet.md`."                                      | Fix for demo PRs: PR comment or PR body link to the packet artifact MUST be required, not optional. "Optional" applies to non-demo usage only.                                                                                                                                                                                                                           |
| G6  | major    | spec.md / Required Demo Evidence  | FR-006 says "Each feature PR MUST have a GitHub Actions check or an explicit `cannot_verify` verification state." For retroactive packets on existing PRs, the CI run is historical. The spec doesn't distinguish historical-CI citation from new-CI requirement.    | Add: for retroactive packets, verification evidence cites the historical CI run URL and artifact availability at packet-generation time. If the CI run exists but its artifacts expired, the row is `partial` with gap `artifact_expired`, not `pass`. New v2 PRs MUST have fresh CI runs.                                                                                  |
| G7  | major    | plan.md / Workstream B ordering   | B2 says "one packet generated from a selected existing feature PR" comes after B3 (setup PR), but doesn't clarify whether the first packet is generated from the setup PR's own CI or from a separate retroactive action. Ambiguity blocks implementation.            | Clarify ordering: B3 setup PR establishes CI artifact upload and demo tracker. B4 first action is a retroactive-packet PR that generates the first packet from selected existing evidence using local tooling, then uploads it as a new CI artifact on that PR. The setup PR itself does not generate a packet.                                                           |
| G8  | minor    | demo-repo-plan.md / Files To Add  | `.sdp-trace/packets/.gitkeep` and `.sdp-trace/bundles/.gitkeep` are listed, but the packet/bundle naming convention uses `feature-<number>` which implies directory-per-feature for bundles but flat files for packets. Convention is inconsistent.                   | Clarify: packets are `.sdp-trace/packets/feature-<number>.md` (flat), bundles are `.sdp-trace/bundles/feature-<number>/manifest.json` (directory). Add a note that the directory convention is intentional for bundle extensibility.                                                                                                                                       |
| G9  | minor    | tasks.md / T019                   | "Generate first packet from selected existing feature PR/history" doesn't specify whether this is hand-authored (temporarily acceptable per demo-repo-plan.md) or tool-generated. The transition point from hand-authored to tool-generated packets is not gated.     | Add a gate: T019 may produce a hand-authored packet only if T009-T011 (packet model/renderer) are not yet complete. Once T011 is done, the packet MUST be tool-generated. Record this in the task dependency chain.                                                                                                                                                      |
| G10 | minor    | spec.md / Core Demo Claim         | The demo claim says "sdp-trace can generate a Change Evidence Packet v0" but the first retroactive packet will be hand-authored or locally assembled. The claim overstates until tool-generated packets exist.                                                        | Soften: "sdp-trace can produce a Change Evidence Packet v0" (not "generate") for the first demo. After tool-generated packets exist, upgrade to "generate." Alternatively, mark the first demo as explicitly hand-authored in packet metadata.                                                                                                                            |

Existing repo default:
Yes, existing-repo v2 packetization is the right default, but only if artifact retention is addressed (G2) and retroactive provenance binding rules are added (G1, G6). The existing repo's mess is useful evidence precisely because it has real CI history, real review contamination, and real gaps. A new repo would dodge the product problem. However, if the first selected existing PR has expired CI artifacts and no linked issues, the first packet will be mostly `not_assessed`/`cannot_verify`, which is honest but may underwhelm a CTO. The plan should explicitly prepare the user for that possibility and define when a new v2 PR is the better first packet target.

Smallest first slice:
The smallest acceptable first implementation slice is: (1) implement the Go packet model and Markdown renderer for the 11 required rows with fixture data (A1), (2) generate one hand-authored packet from the best-available existing feature PR with explicit gaps and artifact-expiry checks, (3) upload that packet as a CI artifact on a new "packetization setup" PR in the existing demo repo, and (4) add a PR body link to the packet. This is one PR, one packet, one CTO-readable artifact. It proves the concept without requiring GitHub API import (A2), observation mapping (A3), theater derivation (A4), or full v2 feature PRs (B4). Everything else is follow-on.

Claims to refuse:
- The demo MUST NOT claim `sdp-trace` "generated" the packet if it was hand-authored; say "produced" and record authoring method.
- The demo MUST NOT claim CI verification is `pass` if CI artifacts have expired; `partial` with `artifact_expired` gap is the correct state.
- The demo MUST NOT claim review evidence is present for retroactive packets if the existing review was contaminated (Feature 4); the row must be `partial` or `not_assessed` with the contamination noted.
- The demo MUST NOT claim the existing demo repo's CI history proves v2 packet capability; it proves v1 observation capability, and v2 packetization is the new work.
- The demo MUST NOT present the setup PR as a feature PR; it is infrastructure only.
```
