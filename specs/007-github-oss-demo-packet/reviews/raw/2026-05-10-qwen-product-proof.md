Verdict: REVISE_BEFORE_USER_APPROVAL

Findings:
| id | severity | file/section | finding | exact fix |
| --- | --- | --- | --- | --- |
| 006-P1 | major | tasks.md: Phase 2 tasks | P0 Classification Rule requires `packet_rows`, `evidence_surface`, `start_state`, `target_transition`, `buyer_effect`, `non_goal` for every P0 item; T009-T015 omit all six fields. | Add a P0 intake table to tasks.md mapping each T009-T015 to specific `PC-*` rows, current row state, and the exact buyer_effect sentence. |
| 006-P2 | major | demo-repo-plan.md: "Feature PR Requirements" | No success threshold defines which rows must exit `not_assessed` for a packet to count as demo-success. A packet with all rows `not_assessed` satisfies the letter of the spec but proves nothing to a CTO. | Add a row-state threshold: for the first packet, at least `PC-CHANGE`, `PC-MUTATION`, and one of `PC-VERIFICATION`/`PC-REVIEW` must be `pass` or `partial` with at least one evidence ref resolved. |
| 006-P3 | major | spec.md: "Negative / Theater Demo" | The negative demo says `agent_claimed_verification` **or** `ci_theater` without specifying which is the primary target and what exact trigger evidence demonstrates it. Without a concrete trigger, the theater demo becomes prose about theater. | Specify the primary theater reason code (recommend `ci_theater`), name the exact claim ref, and name the exact missing independent artifact that will cause the finding to trigger. |
| 006-P4 | minor | plan.md: Workstream B2 | "First packet may be generated locally from retained evidence only while the product generator is being implemented" risks hand-authored packets becoming the de facto template, violating Product Contract v0 sectionPacket Generation. | Add a sunset constraint: any locally-generated or hand-authored packet must be regenerated by the product tooling before the demo is considered complete, and must carry a "generated-before-tooling" marker. |
| 006-P5 | minor | spec.md: "Required Demo Evidence" table | Verification row says "GitHub Actions check and retained artifact, or explicit `cannot_verify`" but does not define what happens when GitHub API data and observation-ledger data contradict (e.g., CI shows green but harness observed a failing test). | Add a contradiction rule: when two evidence surfaces disagree, verification state is `partial`, the disagreement is recorded in `evidence refs`, and `PC-RESIDUAL-GAPS` names both refs. |
| 006-P6 | minor | plan.md: A5 PR Projection | No rule for PR comment lifecycle (edit, delete, conflict with uploaded artifact). Product Contract v0 says artifact is canonical, but demo spec does not enforce this. | Add a canonicality rule: PR comment is informational only; if PR comment text conflicts with the uploaded `change-evidence-packet.md`, the uploaded file wins. PR comment must include "artifact canonical" disclaimer. |

Existing repo default:
Yes. Continuing in `sdp-trace-demo-jvm-gsd` through a v2 packetization track is the right default. A sterile new repo sidesteps the exact product question: can `sdp-trace` make a real agent-delivery history legible? The v1 tag -> v2 milestone path correctly preserves history as evidence rather than hiding it. However, the spec must not let this correct decision become an excuse for an unfaceted first slice (see 006-P2 and 006-P3).

Smallest first slice:
Produce **one** Change Evidence Packet from a **single selected existing feature PR** in the current repo, with the following constraints: (1) map all 11 required `PC-*` rows to current evidence, (2) at least `PC-CHANGE` and `PC-MUTATION` must resolve to concrete GitHub evidence refs, (3) at least one row among `PC-VERIFICATION`/`PC-REVIEW`/`PC-AGENT-ROUTE` must exit `not_assessed`, (4) all remaining rows remain `not_assessed` or `cannot_verify` with a one-line reason, (5) generate an evidence bundle manifest with at least three entries, (6) produce the canonical Markdown packet file. No theater demo, no five-feature sequence, no CI wiring, and no PR comment projection are in scope. This slice proves the packet shape, not the demo flow.

Claims to refuse:
- **Do not claim** that a generated packet with all `not_assessed` rows constitutes a successful demo. A packet full of gaps is honest, but it does not prove CTO readability.
- **Do not claim** that five feature PRs prove the product before one feature packet is read by a CTO proxy. Volume is not proof.
- **Do not claim** that GitHub CI green status equals `PC-VERIFICATION: pass` without a retained artifact ref in the bundle manifest.
- **Do not claim** that the existing repo PR history is complete evidence for all 11 packet rows. Many rows will be `not_assessed`; the packet must say so explicitly.
- **Do not claim** that the v2 packet path retroactively makes v1 runs compliant. v1 is evidence substrate; it is not a completed packet.
