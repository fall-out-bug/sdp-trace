# Tasks: GitHub OSS Demo Packet

**Input**: Design documents from `/specs/007-github-oss-demo-packet/`
**Prerequisites**: 006 Change Evidence Packet Core approved and implemented to
schema, validator, renderer, CLI validate/render behavior, and required
fixtures.
**Tests**: Spec-only slice. Verification is Markdown sanity, `git diff
--check`, repo baseline `go test ./...`, `jq empty schema/*.json`, and Socratic
review before implementation approval.

## Phase 0: Spec Draft

- [x] T001 Create `specs/007-github-oss-demo-packet/`.
- [x] T002 Draft `spec.md` defining the GitHub-first OSS demo claim, non-claims,
  demo-track policy, required demo evidence, and success criteria.
- [x] T003 Draft `plan.md` making 006 the prerequisite and limiting 007 to demo
  repository v2 work.
- [x] T004 Draft `demo-repo-plan.md` with existing-repo v2 packetization,
  feature packet requirements, negative theater PR, and buyer demo script.
- [x] T005 Draft `tasks.md`.

## Phase 1: Review Before Implementation

- [x] T006 Run Socratic review of the 007 spec package across product proof,
  demo contamination, GitHub-first scope, and theater semantics.
- [x] T007 Fix or explicitly defer all critical/major review findings and record
  dispositions in `reviews/2026-05-10-socratic-review.md`.
  Evidence: `reviews/2026-05-10-socratic-review.md`, including the 2026-05-26
  focused split re-review.
- [ ] T008 Ask for explicit approval of:
  - demo-track option;
  - first implementation slice;
  - whether to continue in the existing demo repo or create a later polished
    sales repo.

## Phase 2: Demo Repository V2 After 006 Completion

- [ ] T009 Confirm 006 product slice is available in the demo environment:
  schema, validator, renderer, CLI validate/render behavior, and fixtures.
- [ ] T010 Preserve current demo v1 history with tag or archival note.
- [ ] T011 Create `demo-v2-packetization` tracker in the existing demo repo.
- [ ] T012 Create packetization setup PR with artifact upload, PR template, and
  demo tracker. Setup artifact is `setup_only`, not feature proof. Add a
  machine-checkable file-scope guard or independent review record proving the
  setup PR did not change application feature behavior.
- [ ] T013 For the first selected feature, prepare `sdp-trace` recorder setup
  outside the OpenCode/GSD developer prompt. The prompt must not ask the route
  to use `sdp-trace`, write evidence, update trace files, or close packet rows.
- [ ] T014 Generate first packet from selected existing OpenCode/GSD feature
  PR/history, or create a new v2 feature PR implemented by OpenCode/GSD +
  MiniMax if no existing feature meets the minimum bar. The packet must be
  generated by the 006 renderer and validated by the 006 validator. Before
  using existing/backfilled history as CTO-visible route proof, audit whether
  any feature behavior or repair was Codex-authored.
- [ ] T015 Create or backfill feature 1 packet and bundle. Codex-authored
  feature behavior cannot close feature rows. Retain prompt text or prompt
  digest metadata and validate that the prompt did not ask OpenCode/GSD to use
  `sdp-trace`, write evidence, update trace files, or close packet rows.
- [ ] T016 Create or backfill feature 2 packet and bundle. Codex-authored
  feature behavior cannot close feature rows. Retain prompt text or prompt
  digest metadata and validate that the prompt did not ask OpenCode/GSD to use
  `sdp-trace`, write evidence, update trace files, or close packet rows.
- [ ] T017 Create or backfill feature 3 packet and bundle. Codex-authored
  feature behavior cannot close feature rows. Retain prompt text or prompt
  digest metadata and validate that the prompt did not ask OpenCode/GSD to use
  `sdp-trace`, write evidence, update trace files, or close packet rows.
- [ ] T018 Create or backfill feature 4 packet and bundle. Must surface known
  review contamination or exclude Feature 4 from CTO-visible packet selection;
  Codex-authored feature behavior cannot close feature rows. Retain prompt text
  or prompt digest metadata and validate that the prompt did not ask OpenCode/GSD
  to use `sdp-trace`, write evidence, update trace files, or close packet rows.
- [ ] T019 Create or backfill feature 5 packet and bundle. Codex-authored
  feature behavior cannot close feature rows. Retain prompt text or prompt
  digest metadata and validate that the prompt did not ask OpenCode/GSD to use
  `sdp-trace`, write evidence, update trace files, or close packet rows.
- [ ] T020 Create `DEMO-NEGATIVE:` theater draft PR with packet and bundle.
  Primary reason code: `agent_claimed_verification`. If 006 validates but does
  not automatically detect the finding, record that limitation in
  `PC-RESIDUAL-GAPS`.
- [ ] T021 Run a first-packet gate before buyer rehearsal. It must validate the
  packet with 006 tooling and check the 007 minimum bar, including
  `PC-AGENT-ROUTE: pass|partial` backed by recorder-observed
  OpenCode/GSD/MiniMax evidence. Failure blocks demo success.
- [ ] T022 Run buyer-demo rehearsal against one happy-path PR and one negative
  PR.

## Demo Feature Route Rule

For CTO-visible feature work, Codex may coordinate, review, inspect, run
verification, and fix P0 `sdp-trace` product blockers. Codex must not implement
or repair demo feature behavior directly. Required feature implementation route:
OpenCode + GSD + `minimax-coding-plan/MiniMax-M2.5` under passive `sdp-trace`
observation.

If a feature fails, Codex redirects OpenCode/GSD with a new task or prompt. If
the recorder cannot prove the route, P0 `sdp-trace` issues are fixed before
claiming the feature packet; P1 and lower issues are recorded and can follow
after feature delivery unless they block the demo proof.

At least one happy-path CTO-visible feature packet must have
`PC-AGENT-ROUTE: pass` or `partial` backed by recorder-observed
OpenCode/GSD/MiniMax evidence before demo success can be claimed.

Checked-in recorder artifacts are not authority by themselves. Each feature
packet must either use live-validated or CI-retained recorder artifacts with
resolver refs and digests, or explicitly mark the affected proof area
`cannot_verify`/`partial` with a residual gap. Prompt/setup metadata alone may
support route proof only as `partial`; `PC-AGENT-ROUTE: pass` requires stronger
machine evidence of recorder integrity and passivity.

## Completion Rule

This slice is not implementation approval. It is complete only when the spec is
reviewed and the user explicitly approves the demo-track option and first
implementation slice.
